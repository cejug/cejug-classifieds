<project>
	<property environment="env" />
	<property name="as.home" value="${env.AS_HOME}" />
	<property name="jaxws.lib.home" value="${env.JAXWS_HOME}/lib" />
	<property name="project.lib" value="${basedir}/lib" />
	<property name="build.home" value="${basedir}/build" />
	<property name="build.ear.home" value="${build.home}/ear" />
	<property name="build.entity.package" value="${build.home}/entities" />
	<property name="build.classes.home" value="${build.home}/classes" />
	<property name="domain" value="domain1" />
	<target name="deploy" depends="deploy-tomcat, deploy-appserver" />

	<target name="deploy-appserver" unless="tomcat">
		<copy file="${build.ear.home}/${ant.project.name}.war" todir="${as.home}/domains/${domain}/autodeploy" />
	</target>

	<target name="deploy-tomcat" if="tomcat">
		<copy file="${build.ear.home}/jaxws-${ant.project.name}.war" todir="${env.CATALINA_HOME}/webapps" />
	</target>

	<target name="help">
		<echo message="server:       Builds and deploy the service endpoint WAR" />
		<echo message="client:       Builds the client" />
		<echo message="run:          Runs the client" />
	</target>
</project>
